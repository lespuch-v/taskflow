# This project uses docker-compose to run both frontend and backend.
# See docker-compose.yml in this directory.
#
# Individual Dockerfiles:
#   - TaskFlow.Api/Dockerfile      (backend)
#   - taskflow-client/Dockerfile   (frontend)
#
# Usage:
#   docker compose up --build
